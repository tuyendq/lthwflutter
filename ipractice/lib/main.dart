import 'package:flutter/material.dart';

void main() {
  runApp(
    const Center(
      child: Text(
        '"Everything is practice." — Pelé',
        textDirection: TextDirection.ltr,
      ),
    ),
  );
}
